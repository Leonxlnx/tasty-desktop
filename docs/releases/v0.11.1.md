# Tasty 0.11.1

Tasty 0.11.1 fixes two Windows startup regressions found after the 0.11 release.

- Declares the complete Tasty icon set explicitly for Windows bundles.
- Retries one transient Windows `spawn EPERM` when restoring a provider session.
- Keeps persistent or unrelated permission errors visible instead of hiding them.
